# 📦 Сервис мониторинга отправлений СДЭК

Веб-сервис для отслеживания статусов отправлений СДЭК с автоматическим определением проблемных посылок.

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15+-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)

---

## 📑 Содержание

- [🚀 Возможности](#-возможности)
- [📋 Требования](#-требования)
- [🛠️ Установка и запуск](#️-установка-и-запуск)
  - [🐳 Вариант 1: Docker Compose (Рекомендуется)](#-вариант-1-docker-compose-рекомендуется)
  - [🔧 Вариант 2: Ручная установка](#-вариант-2-ручная-установка)
- [📖 Использование](#-использование)
- [🔍 Правило определения проблемных отправлений](#-правило-определения-проблемных-отправлений)
- [🗄️ Структура базы данных](#️-структура-базы-данных)
- [📁 Структура проекта](#-структура-проекта)
- [🐛 Устранение неполадок](#-устранение-неполадок)
- [📚 Дополнительная документация](#-дополнительная-документация)

---

## 🚀 Возможности

- ✅ Интеграция с API СДЭК (OAuth 2.0)
- ✅ Хранение истории всех статусов отправлений
- ✅ Веб-интерфейс с статистикой и таблицей отправлений
- ✅ JSON API для получения данных
- ✅ Автоматическое определение проблемных отправлений
- ✅ Обновление статусов по запросу
- ✅ Docker-развертывание с автоматической инициализацией
- ✅ Автоматическое создание тестовых заказов

## 📋 Требования

- **Вариант 1 (Docker):** Docker и Docker Compose
- **Вариант 2 (Ручной):** Python 3.11+, PostgreSQL 15+
- **Обязательно:** Учетные данные API СДЭК

## 🛠️ Установка и запуск

> 💡 **Рекомендуем начать с Docker** - всё настроится автоматически за 2 минуты!

---

## 🐳 Вариант 1: Docker Compose (Рекомендуется)

### Преимущества:
- ✅ Автоматическое развертывание PostgreSQL
- ✅ Автоматический запуск миграций
- ✅ Автоматическое создание 3 тестовых заказов
- ✅ Автоматическое получение трек-номеров
- ✅ Готовое приложение с данными за 2 минуты

### Шаг 1: Подготовка

```bash
# Скопируйте пример конфигурации
cp .env.docker.example .env

# Отредактируйте .env и укажите ваши учетные данные СДЭК
# CDEK_CLIENT_ID=ваш_client_id
# CDEK_CLIENT_SECRET=ваш_client_secret
# CDEK_API_URL=https://api.edu.cdek.ru/v2  # Тестовое окружение
```

### Шаг 2: Запуск

```bash
# Запустите все сервисы
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

### Шаг 3: Готово! 🎉

Откройте браузер: **http://localhost:8000/shipments**

Вы увидите 3 тестовых отправления с реальными трек-номерами!

### Что произошло автоматически:

1. ✅ PostgreSQL развернут на порту 5436
2. ✅ Миграции применены
3. ✅ Созданы 3 тестовых заказа через CDEK API
4. ✅ Получены трек-номера (cdek_number)
5. ✅ Трек-номера добавлены в БД
6. ✅ FastAPI приложение запущено на порту 8000

### Полезные команды:

```bash
# Просмотр логов
docker-compose logs -f

# Просмотр логов только приложения
docker-compose logs -f web

# Остановка
docker-compose down

# Полная очистка (включая БД)
docker-compose down -v

# Перезапуск с новыми данными
docker-compose down -v && docker-compose up --build
```

---

## 🔧 Вариант 2: Ручная установка

### Преимущества:
- ✅ Полный контроль над конфигурацией
- ✅ Возможность использовать существующую БД
- ✅ Подходит для разработки и кастомизации

### Шаг 1: Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Шаг 2: Настройка PostgreSQL

```bash
# Создайте базу данных
createdb -U postgres delivery_monitoring

# Или через psql:
psql -U postgres
CREATE DATABASE delivery_monitoring;
\q
```

📖 **Подробная инструкция:** [POSTGRESQL_SETUP.md](POSTGRESQL_SETUP.md)

### Шаг 3: Настройка переменных окружения

```bash
# Скопируйте пример
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Учетные данные СДЭК API
CDEK_CLIENT_ID=ваш_client_id
CDEK_CLIENT_SECRET=ваш_client_secret

# Тестовое окружение (рекомендуется для начала)
CDEK_API_URL=https://api.edu.cdek.ru/v2

# Продакшн окружение (когда будете готовы)
# CDEK_API_URL=https://api.cdek.ru/v2

# База данных PostgreSQL
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/delivery_monitoring
```

### Шаг 4: Получение учетных данных СДЭК

1. Зарегистрируйтесь: https://www.cdek.ru/
2. Перейдите: "Интеграция" → "API"
3. Создайте приложение
4. Скопируйте `client_id` и `client_secret`

📖 **Документация API:** https://apidoc.cdek.ru/

### Шаг 5: Инициализация базы данных

```bash
# Применение миграций
alembic upgrade head
```

### Шаг 6: Создание тестовых заказов

**Вариант А: Автоматическое создание (рекомендуется)**

```bash
# Создать 3 тестовых заказа и получить трек-номера
python create_test_orders.py

# Добавить полученные трек-номера в БД
python init_db.py
```

Скрипт `create_test_orders.py`:
- Создаст 3 заказа через CDEK API
- Получит трек-номера (cdek_number или im_number)
- Сохранит их в файл `test_orders.txt`

Затем скопируйте трек-номера из вывода в `init_db.py`:

```python
# В файле init_db.py замените:
test_tracking_codes = [
    "1106698104",  # Ваши реальные номера из create_test_orders.py
    "1106698105",
    "1106698107",
]
```

**Вариант Б: Использование реальных трек-номеров**

Если у вас уже есть трек-номера из ЛК СДЭК:

```python
# Отредактируйте init_db.py
test_tracking_codes = [
    "ваш_трек_номер_1",
    "ваш_трек_номер_2",
    "ваш_трек_номер_3",
]
```

Затем запустите:

```bash
python init_db.py
```

### Шаг 7: Запуск приложения

```bash
# Запуск сервера разработки
uvicorn app.main:app --reload

# Или через run.py
python run.py
```

Откройте браузер: **http://localhost:8000/shipments**

---

## 📖 Использование

### Веб-интерфейс

Откройте в браузере: http://localhost:8000/shipments

**Функционал:**
- Статистика: всего отправлений, в пути, доставлено, проблемных
- Таблица со всеми отправлениями
- Кнопка "Обновить статусы" для получения актуальных данных из API СДЭК

### API Эндпоинты

#### 1. Получение списка отправлений (JSON)

```bash
GET /api/shipments
```

**Пример ответа:**
```json
[
  {
    "id": 1,
    "tracking_code": "1234567890123",
    "created_at": "2024-01-15T10:30:00",
    "current_status": "В пути (Москва)",
    "current_status_datetime": "2024-01-16T14:20:00",
    "problem": false
  },
  {
    "id": 2,
    "tracking_code": "9876543210987",
    "created_at": "2024-01-10T08:15:00",
    "current_status": "Ожидает в пункте выдачи (Санкт-Петербург)",
    "current_status_datetime": "2024-01-12T12:00:00",
    "problem": true
  }
]
```

#### 2. Обновление статусов всех отправлений

```bash
POST /update-statuses
```

**Пример ответа:**
```json
{
  "success": true,
  "total_shipments": 3,
  "updated_successfully": 3,
  "failed": 0,
  "total_new_statuses": 5,
  "details": [
    {
      "success": true,
      "tracking_code": "1234567890123",
      "new_statuses": 2,
      "total_statuses": 5
    }
  ]
}
```

#### 3. Health Check

```bash
GET /health
```

## 🔍 Правило определения проблемных отправлений

Отправление считается **проблемным**, если выполняются оба условия:

1. ❌ Текущий статус НЕ является статусом доставки
2. ⏰ С момента первого статуса прошло более 3 календарных дней

**Статусы доставки:**
- `DELIVERED` - Доставлено
- `RECEIVED_AT_DELIVERY_OFFICE` - Получено в пункте выдачи

## 🗄️ Структура базы данных

### Таблица `shipments`

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| tracking_code | String(100) | Уникальный трек-номер |
| created_at | DateTime | Дата создания записи |

### Таблица `shipment_statuses`

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| shipment_id | Integer | FK → shipments.id |
| status_code | String(50) | Код статуса из API СДЭК |
| status_text | Text | Текстовое описание статуса |
| status_datetime | DateTime | Время статуса по данным СДЭК |
| created_at | DateTime | Дата создания записи |

## 🔐 Авторизация в API СДЭК

Сервис использует OAuth 2.0 Client Credentials Flow:

1. При первом запросе сервис получает access token
2. Token кэшируется и переиспользуется до истечения срока действия
3. При истечении автоматически запрашивается новый token

**Эндпоинт авторизации:**
```
POST https://api.cdek.ru/v2/oauth/token
```

**Параметры:**
- `grant_type=client_credentials`
- `client_id` - из переменной окружения
- `client_secret` - из переменной окружения

## 📁 Структура проекта

```
delivery_monitoring/
├── alembic/                       # Миграции БД
│   ├── versions/                 # Файлы миграций
│   ├── env.py                    # Конфигурация Alembic
│   └── script.py.mako            # Шаблон миграций
├── app/
│   ├── templates/                # HTML шаблоны
│   │   └── shipments.html        # Главная страница
│   ├── __init__.py
│   ├── config.py                 # Настройки приложения
│   ├── database.py               # Подключение к БД
│   ├── models.py                 # SQLAlchemy модели
│   ├── cdek_client.py            # Клиент API СДЭК
│   ├── services.py               # Бизнес-логика
│   ├── logging_config.py         # Конфигурация логирования
│   └── main.py                   # FastAPI приложение
├── logs/                          # Логи приложения
│   └── app.log                   # Основной лог-файл
├── .env                          # Переменные окружения (не в git)
├── .env.example                  # Пример для ручной установки
├── .env.docker.example           # Пример для Docker
├── .gitignore
├── alembic.ini                   # Конфигурация Alembic
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Docker образ
├── docker_init.py                # Скрипт инициализации для Docker
├── init_db.py                    # Скрипт добавления трек-номеров в БД
├── create_test_orders.py         # Создание тестовых заказов через API
├── run.py                        # Запуск приложения
├── requirements.txt              # Зависимости Python
├── README.md                     # Основная документация
```

## 🧪 Тестирование

### Ручное тестирование

1. Запустите сервер: `uvicorn app.main:app --reload`
2. Откройте http://localhost:8000/shipments
3. Нажмите "Обновить статусы"
4. Проверьте обновление данных в таблице

### Проверка API

```bash
# Получение списка отправлений
curl http://localhost:8000/api/shipments

# Обновление статусов
curl -X POST http://localhost:8000/update-statuses
```

## 🐛 Устранение неполадок

### Docker

#### Порт уже занят

```
Error: port is already allocated
```

**Решение:** Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "5437:5432"  # БД
  - "8001:8000"  # Приложение
```

#### Ошибка авторизации при инициализации

```
❌ Ошибка авторизации: 401
```

**Решение:**
1. Проверьте `.env` файл
2. Убедитесь, что `CDEK_CLIENT_ID` и `CDEK_CLIENT_SECRET` правильные
3. Перезапустите: `docker-compose down && docker-compose up --build`

#### Не создались тестовые заказы

**Решение:**
1. Проверьте логи: `docker-compose logs init`
2. Убедитесь, что API доступен
3. Попробуйте создать заказы вручную: `python create_test_orders.py`

### Ручная установка

#### Ошибка подключения к БД

```
sqlalchemy.exc.OperationalError: could not connect to server
```

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env`
3. Проверьте пароль и имя БД

#### Ошибка авторизации СДЭК

```
401 Unauthorized
```

**Решение:** 
- Проверьте `CDEK_CLIENT_ID` и `CDEK_CLIENT_SECRET`
- Убедитесь, что используете правильный `CDEK_API_URL` (prod/test)
- Проверьте, что учетные данные активны в ЛК СДЭК

#### Нет данных о статусах

**Возможные причины:**
- Неверные трек-номера
- Трек-номера из другого окружения (prod/test)
- Используется `cdek_number` вместо `im_number` (для тестового API)
- Отправления еще не зарегистрированы в системе СДЭК

**Решение:**
1. Проверьте логи: `tail -f logs/app.log`
2. Используйте скрипт проверки: `python test_single_tracking.py`
3. Для тестового API используйте `im_number` в `app/cdek_client.py`

## 📝 Дополнительные команды

### Работа с миграциями

```bash
# Создание новой миграции
alembic revision --autogenerate -m "описание изменений"

# Применение всех миграций
alembic upgrade head

# Откат последней миграции
alembic downgrade -1

# Просмотр истории миграций
alembic history
```

### Очистка БД

```bash
# Откат всех миграций
alembic downgrade base

# Повторное применение
alembic upgrade head
```

## 🔗 Полезные ссылки

- **CDEK API Документация:** https://apidoc.cdek.ru/
- **Личный кабинет СДЭК:** https://www.cdek.ru/
- **FastAPI Документация:** https://fastapi.tiangolo.com/
- **SQLAlchemy Документация:** https://docs.sqlalchemy.org/
- **Alembic Документация:** https://alembic.sqlalchemy.org/

## 🎯 Быстрые команды

### Docker
```bash
# Запуск
docker-compose up -d --build

# Логи
docker-compose logs -f web

# Остановка
docker-compose down

# Полная очистка
docker-compose down -v
```

### Ручная установка
```bash
# Активация окружения
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Создание тестовых заказов
python create_test_orders.py

# Добавление в БД
python init_db.py

# Запуск приложения
python run.py

# Просмотр логов
tail -f logs/app.log  # Linux/Mac
Get-Content logs\app.log -Wait -Tail 50  # Windows PowerShell
```
